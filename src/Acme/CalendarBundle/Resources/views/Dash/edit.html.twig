<link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}" type="text/css" media="screen" />
<script type="text/javascript" src="{{ asset('js/jquery.js') }}"></script>
<script type="text/javascript" src="{{ asset('js/jquery-ui.min.js') }}"></script>
<script type="text/javascript" src="{{ asset('js/bootstrap-modal.js') }}"></script>

<a class="btn pull-right"  href="{{ path('AcmeCalendarBundle_dash_list_calendars') }}" ><i class="icon-chevron-left"></i> Back</a>
<div id="myModal" class="modal hide fade" style="display: none; ">
   <div class="modal-header">
     <a class="close" data-dismiss="modal">×</a>
     <h3>Delete Task?</h3>
   </div>
   <div class="modal-body">
     <p>Are you sure you want to delete this task?</p>
   </div>
   <div class="modal-footer">
   <form action="{{ path('AcmeTaskBundle_delete') }}" method="POST">
   	<input type="hidden" name="id" value="{{ id }}" />
     <button type="submit" class="btn btn-primary">Save changes</button>
        <a href="#" class="btn" data-dismiss="modal">Close</a>
     </form>
   </div>
 </div>
<a data-toggle="modal" href="#myModal" style="float: left;" class="btn btn-danger" ><i class="icon-trash icon-white"></i> Delete</a>

{% if app.session.hasFlash('question') %}
	<div class="info message">
		<p>{{ app.session.flash('question') }}</p>
	</div>
{% endif %}

{% form_theme form 'AcmeTaskBundle:Dash:fields.html.twig' %}
<form class="thumbnail form-horizontal" action="{{ path('AcmeCalendarBundle_dash_edit_calendar' , { "id": id}) }}" method="post" {{ form_enctype(form) }}>
<fieldset>
	<legend>Edit Calendar</legend>
	{{ form_widget(form) }}
	<div class="form-actions">
		<button type="submit" class="btn btn-primary">Save changes</button>
		<button type="reset" class="btn">Cancel</button>
	</div>
</fieldset>    
</form>
